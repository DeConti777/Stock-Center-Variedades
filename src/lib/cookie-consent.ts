/** Chave em `localStorage` — manter alinhado com o banner e o analytics. */
export const COOKIE_CONSENT_STORAGE_KEY = "stock-center-cookie-consent-v1";

/** Disparado na mesma aba quando o usuario aceita o banner (para carregar GA sem reload). */
export const COOKIE_CONSENT_ACCEPTED_EVENT = "stock-center-cookie-consent-accepted";
